/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,
  
  images: {
    domains: ["via.placeholder.com", "img.freepik.com", "images.unsplash.com"],
  },
};

export default nextConfig;
